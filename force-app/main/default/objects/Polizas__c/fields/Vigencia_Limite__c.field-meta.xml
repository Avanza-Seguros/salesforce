<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Vigencia_Limite__c</fullName>
    <formula>IF(
  OR(
    ISPICKVAL(Estatus__c, &quot;Vigor&quot;),
    ISPICKVAL(Estatus__c, &quot;Traspaso&quot;)
  ),
  IF(
    NOT(ISBLANK(Fecha_de_Emision__c)),
    IF(
      DATE(YEAR(TODAY()), MONTH(Fecha_de_Emision__c), DAY(Fecha_de_Emision__c)) &gt;= TODAY(),
      DATE(YEAR(TODAY()), MONTH(Fecha_de_Emision__c), DAY(Fecha_de_Emision__c)),
      DATE(YEAR(TODAY()) + 1, MONTH(Fecha_de_Emision__c), DAY(Fecha_de_Emision__c))
    ),
    null
  ),
  null
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Vigencia Limite</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Date</type>
</CustomField>
